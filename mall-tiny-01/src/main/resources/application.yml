spring:
  datasource:
    url: jdbc:mysql://localhost/mall?useUnicode=true&characterEncoding=utf-8&&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver


mybatis:
  mapper-locations:
    - classpath:mapper/*.xml
    - classpath*:com/**/mapper/*.xml
